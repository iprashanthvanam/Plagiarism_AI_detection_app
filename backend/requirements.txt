

# fastapi==0.103.2
# uvicorn==0.23.2

# asyncpg==0.29.0
# sqlalchemy==2.0.23
# psycopg2-binary==2.9.9

# passlib==1.7.4
# bcrypt==4.0.1
# python-jose[cryptography]==3.3.0
# python-dotenv==1.0.0
# python-multipart==0.0.9

# aiofiles==23.2.1
# pydantic==1.10.26

# requests
# beautifulsoup4
# newspaper3k
# warcio

# pandas
# openpyxl
# xlrd==1.2.0
# python-docx
# python-pptx
# pdfplumber
# pillow

# scikit-learn
# nltk
# torch
# transformers

# google-genai

# six











# --- Core API ---
fastapi>=0.110
starlette>=0.36
uvicorn>=0.27

# --- Async / Validation ---
pydantic>=2.6
anyio>=4

# --- Database ---
asyncpg==0.29.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# --- Auth / Security ---
passlib==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
python-dotenv==1.0.0
python-multipart==0.0.9

# --- File handling ---
aiofiles==23.2.1
pillow
pdfplumber
python-docx
python-pptx
pandas
openpyxl
xlrd>=2.0.1

# --- Scraping / Web ---
requests
beautifulsoup4
newspaper3k
warcio

# --- ML / NLP ---
scikit-learn
nltk
torch
transformers

# --- Gemini OCR ---
google-genai

slowapi



# ============================================================
# ADD THESE TO YOUR backend/requirements.txt
# ============================================================

# Local OCR for images and scanned PDFs (no quota, no API key needed)
pytesseract>=0.3.10        # Python wrapper for Tesseract OCR engine
Pillow>=10.0.0             # Image processing (required by pytesseract)
pdf2image>=1.17.0          # Convert PDF pages to images for Tesseract

# Local .doc extraction (legacy binary Word format)
mammoth>=1.6.0             # Extracts raw text from .doc files cleanly

# ============================================================
# SYSTEM PACKAGES (install on your Ubuntu machine)
# Run these once in terminal:
# ============================================================
#
#   sudo apt-get update
#   sudo apt-get install -y tesseract-ocr poppler-utils
#
# tesseract-ocr  → the actual Tesseract OCR engine (pytesseract needs this)
# poppler-utils  → pdf2image needs this to render PDF pages (pdfinfo, pdftoppm)
#
# Verify installation:
#   tesseract --version
#   pdftoppm -h
#
# ============================================================
# OPTIONAL: Additional language packs for Tesseract
# (default is English only — add if your PDFs are in other languages)
# ============================================================
#   sudo apt-get install -y tesseract-ocr-hin   # Hindi
#   sudo apt-get install -y tesseract-ocr-tel   # Telugu
#   sudo apt-get install -y tesseract-ocr-tam   # Tamil
#   sudo apt-get install -y tesseract-ocr-ben   # Bengali
#   sudo apt-get install -y tesseract-ocr-mal   # Malayalam
#   sudo apt-get install -y tesseract-ocr-guj   # Gujarati
#   sudo apt-get install -y tesseract-ocr-kan   # Kannada
#   sudo apt-get install -y tesseract-ocr-mar   # Marathi
#   sudo apt-get install -y tesseract-ocr-ori   # Oriya
#   sudo apt-get install -y tesseract-ocr-pun   # Punjabi
#   sudo apt-get install -y tesseract-ocr-ben   # Bengali




# ============================================================
# ADD THESE NEW LINES to your existing requirements.txt
# (these are the only additions needed for the new plagiarism.py)
# ============================================================

# [B1] Sentence-BERT — semantic/paraphrasing detection
# Downloads ~80MB model (all-MiniLM-L6-v2) on first run
# Cached at ~/.cache/huggingface/ — no re-download after that
sentence-transformers>=2.7.0

# numpy is auto-installed by scikit-learn and torch, but pin here for clarity
numpy>=1.24.0

# ============================================================
# ALL OTHER PACKAGES already exist in your requirements.txt:
# ============================================================
# scikit-learn   → used by A1 TF-IDF (TfidfVectorizer)
# nltk           → tokenization (already present)
# torch          → required by sentence-transformers (already present)
# transformers   → required by sentence-transformers (already present)
# ============================================================
# NO SYSTEM PACKAGES NEEDED for the new methods
# (Winnowing, Jaccard, Character N-gram = pure Python)
# ============================================================




# OCR — Primary (deep learning, multilingual)
paddlepaddle
paddleocr

# PDF page rendering (required by PaddleOCR/Tesseract page-level fallback)
pdf2image

# OCR — Last resort fallback
pytesseract
Pillow